# Dependências
node_modules/
**/node_modules/
.bun/

# Segredos / ambiente — ignora os .env reais em qualquer pasta; versiona só os *.example
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example
!.env.prod.example
!**/.env.prod.example

# Stack de produção PREENCHIDO (com segredos) — cole no Portainer, NUNCA commite
docker-stack.prod.yml

# Build / artefatos
dist/
build/
*.tsbuildinfo

# Traefik / certificados
letsencrypt/
acme.json

# SO / editor / logs
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Temporários
*.tmp
scratchpad/
